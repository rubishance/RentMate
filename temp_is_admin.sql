CREATE OR REPLACE FUNCTION public.is_admin() RETURNS BOOLEAN LANGUAGE sql SECURITY DEFINER SET search_path = public AS 'SELECT EXISTS ( SELECT 1 FROM public.user_profiles WHERE id = auth.uid() AND role = ''admin''::user_role );';
